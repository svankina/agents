## Local GitLab remote

For a local project's internal GitLab remote, use remote name `gitlab` and
`ssh://git@localhost:2222/root/<repo>.git`. Use current configuration and
focused project documentation for installation or administration details.
