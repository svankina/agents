@~/.omp/agent/AGENTS.md

# gem profile

All-Gemini omp: `google-antigravity/gemini-3.8-flash` for every role
(default, plan, slow, task, smol, vision, commit, advisor, designer) and every
subagent. Exists to exercise the Google OAuth quota instead of Anthropic. No
domain specialization — general coding/ops work under the shared instructions
above.
