# Claude Code slash commands

Custom `/` commands. Each `.md` file is loaded into Claude Code and exposed as a skill.

Format: YAML frontmatter (description, argument-hint) + markdown body.

See [`arch_scan.md`](arch_scan.md) for an example.
