# <Project> Claude Design Audit

Audited via browser automation at `<entrypoint>`.

Artifacts:
- `audit-results.json`: per-route URL/title/style/status checks
- `screenshots/*.png`: captured browser screenshots
- `<project>-contact-sheet.jpg`: visual contact sheet

## Result summary

### Matches the Claude Design direction
- `<route>`

### Partial match / adjacent style
- `<route>`

### Does not match when visited through the live entrypoint
- `<route>`

### Error / auth / offline
- `<route>` — <reason>

## Important notes

- Audit reflects what a user actually sees through `<entrypoint>`, not merely what exists in source.
- If source and live output differ, identify the running service/process/deployment home before changing more files.

## Follow-up plan

1. <fix>
2. <rerun audit>
